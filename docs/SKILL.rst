Claude Code ``SKILL.md``
========================

The source code repository for this project contains a ``SKILL.md`` that can be installed using the ``.scripts/install-skill`` script (or you can manually install the ``SKILL.md`` located at the repo root.)  The script will symlink ``SKILL.md`` so updates are immediately available whenever you get latest from source control.
